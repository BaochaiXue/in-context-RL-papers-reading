# Emergence Of In Context Reinforcement Learning From Noise Distillation

- Paper folder: `papers/Emergence of In-Context Reinforcement Learning from Noise Distillation`
- Note slug: `emergence-of-in-context-reinforcement-learning-from-noise-distillation`

## Full citation
Authors could not be extracted reliably from the local source. Emergence Of In Context Reinforcement Learning From Noise Distillation. Local manuscript in repository, 2023.
## Parsing status
- Primary source type: `source-only`
- Metadata source: `papers/Emergence of In-Context Reinforcement Learning from Noise Distillation/icml2024.tex`
- Issue: No top-level manuscript PDF found; note built from source files or existing cached parse only.

## 5-bullet thesis
- Addresses: Recently, extensive studies in Reinforcement Learning have been carried out on the ability of transformers to adapt in-context to various environments and tasks. Current in-context RL methods are limited by their strict requirements for data, which needs to be generated by RL agents or labeled with actions from an optimal policy.
- Core mechanism: 提出 noise-induced curriculum 来合成 learning histories，降低对最优策略或强行为策略的依赖。
- Primary bottleneck: 方法高度依赖噪声课程设计；噪声分布若不合适，就无法产生可蒸馏的有效学习历史。
- Evaluated on: 在 bandit、DarkRoom 类任务和 pixel-based 域上验证，并与需要最优/次优策略数据的现有 ICRL 方法对比。
- Key failure mode: 当噪声课程与目标任务偏差过大，或者视觉任务难度显著上升时，生成历史的质量会明显下降。

## Problem setting
Recently, extensive studies in Reinforcement Learning have been carried out on the ability of transformers to adapt in-context to various environments and tasks. Current in-context RL methods are limited by their strict requirements for data, which needs to be generated by RL agents or labeled with actions from an optimal policy. In order to address this prevalent problem, we propose AD , a new data acquisition approach that enables in-context Reinforcement Learning from noise-induced curriculum.

## Method summary
提出 noise-induced curriculum 来合成 learning histories，降低对最优策略或强行为策略的依赖。

## Datasets / tasks / benchmarks
在 bandit、DarkRoom 类任务和 pixel-based 域上验证，并与需要最优/次优策略数据的现有 ICRL 方法对比。

## Strongest results
In our experiments, we first show that it is possible to distill the generalized optimal policy from noise, thus making it possible to replace learning histories from RL agents with generated ones. Then we move on to the experiments with suboptimal policies, showing that in-context reinforcement learning is able to outperform them even without the optimal policy. We also test our method on a pixel-based domain and show that it is still able to sustain its abilities despite the more challenging environment. Lastly, we perform an analysis of two factors, (1) what the minimum policy performance should be to enable in-context RL, and (2) the influence of learning pace in data on the final in-context learning performance.

## Failure modes or limitations
当噪声课程与目标任务偏差过大，或者视觉任务难度显著上升时，生成历史的质量会明显下降。

## Evidence anchors
- Introduction
- What Enables In-Context Reinforcement Learning?
- AD$^; varepsilon$
- Experimental Setup
- Model
- Generation of Learning Histories
- Environments
- Evaluation
- Experiments
- Related Work
- Conclusion
- Acknowledges
- Impact Statement
- Comparison with AD and ED
- Learning from extracted policy
- Decreasing starting noise
- More on Watermaze Setup
- Additional Figure for Decay Rate Analysis
- Model Hyperparameters
- Data Hyperparameters
- Data Generation Algorithm

## Tags
`benchmark` `transformers` `meta-rl` `bandits` `memory` `action-models` `icrl`
