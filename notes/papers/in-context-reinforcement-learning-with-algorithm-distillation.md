# In-context Reinforcement Learning with Algorithm Distillation

- Paper folder: `papers/In-context Reinforcement Learning with Algorithm Distillation`
- Note slug: `in-context-reinforcement-learning-with-algorithm-distillation`

## Full citation
Michael Laskin, Luyu Wang, Junhyuk Oh, Emilio Parisotto, Stephen Spencer, Richie Steigerwald, DJ Strouse, Steven Hansen, Angelos Filos, Maxime Gazeau, Himanshu Sahni, Satinder Singh, Volodymyr Mnih. In-context Reinforcement Learning with Algorithm Distillation. Local manuscript in repository, 2022.
## Parsing status
- Primary source type: `source-only`
- Metadata source: `papers/In-context Reinforcement Learning with Algorithm Distillation/main.tex`
- Issue: No top-level manuscript PDF found; note built from source files or existing cached parse only.

## 5-bullet thesis
- Addresses: We propose , a method for distilling reinforcement learning (RL) algorithms into neural networks by modeling their training histories with a causal sequence model. treats learning to reinforcement learn as an across-episode sequential prediction problem.
- Core mechanism: 把 source RL algorithm 的完整 learning history 蒸馏进 causal Transformer，使其测试时仅靠上下文自我改进。
- Primary bottleneck: 主要瓶颈是长 episode 和多 episode context；现有序列模型对超长 horizon 支撑不足。
- Evaluated on: 在 adversarial bandit、DarkRoom、Dark Key-to-Door、Watermaze 上，与 RL^2、Expert Distillation 和 source algorithm 对比。
- Key failure mode: 结论明确指出，一旦任务 episode 很长、需要跨 episode 超长记忆，原始 AD 的上下文建模能力就不够。

## Problem setting
We propose , a method for distilling reinforcement learning (RL) algorithms into neural networks by modeling their training histories with a causal sequence model. treats learning to reinforcement learn as an across-episode sequential prediction problem. A dataset of learning histories is generated by a source RL algorithm, and then a causal transformer is trained by autoregressively predicting actions given their preceding learning histories as context.

## Method summary
把 source RL algorithm 的完整 learning history 蒸馏进 causal Transformer，使其测试时仅靠上下文自我改进。 Over the course of its lifetime a capable rl agent will exhibit complex behaviours, such as exploration, temporal credit assignment, and planning. Our key insight is that an agent's actions, regardless of the environment it inhabits, its internal structure, and implementation, can be viewed as a function of its past experience, which we refer to as its history . Formally, we write: =-1 align H h_ t ( o_ 0 , a_ 0 , r_ 0 , , o_ t-1 , a_ t-1 , r_ t-1 , o_ t , a_ t , r_ t ) = ( o_ t , r_ t , a_ t ) align and we refer to a long history-conditioned policy as an algorithm : align : H O ( A ), align where denotes the space of probability distributions over the action space . eq:algo-signature ~suggests that, similar to a policy, an algorithm can be unrolled in an environment to generate sequences of observations, rewards, and actions.

## Datasets / tasks / benchmarks
在 adversarial bandit、DarkRoom、Dark Key-to-Door、Watermaze 上，与 RL^2、Expert Distillation 和 source algorithm 对比。

## Strongest results
figure [t] subfigure 0.20 assets/images/9x9_1g_dense_main.pdf subfigure subfigure 0.20 assets/images/dark_room_hard_main.pdf subfigure subfigure 0.20 assets/images/dark_key_door_main.pdf subfigure subfigure 0.216 assets/images/watermaze_main_160_epi.pdf subfigure subfigure 0.14 assets/images/legend_main.pdf subfigure Main results: we evaluate , , ED, and the source algorithm on environments that require memory and exploration. In these environments, an agent must reach a goal location that can only be inferred through a binary reward. is consistently able to in-context reinforcement learn across all environments and is more data-efficient than the A3C (``Dark'' environments)~ or DQN (Watermaze)~ source algorithm it distilled. We report the mean return 1 standard deviation over 5 training seeds with 20 test seeds each.

## Failure modes or limitations
结论明确指出，一旦任务 episode 很长、需要跨 episode 超长记忆，原始 AD 的上下文建模能力就不够。

## Evidence anchors
- Introduction
- Background
- Method
- Algorithm Distillation
- Practical Implementation
- Experimental Setup
- Environments
- Baselines
- Evaluation
- Experiments
- Related Work
- Conclusion
- Environment Considerations
- Closely Related Prior Methods
- Expert Distillation Main Results
- Model Size
- Source Algorithm Training Runs
- Label Smoothing Ablation
- RL$^2$ network architecture: Transformer vs LSTM
- Number of Training Tasks in Source Data
- Single-Stream ; fullname
- Random mask
- AD network architecture: Transformer vs LSTM
- fullname Hyperparameters
- Source RL Algorithm Hyperparameters
- Dark Environments
- DMLab Watermaze
- RL$^2$ Hyperparameters
- Attention Maps

## Tags
`benchmark` `transformers` `meta-rl` `exploration` `bandits` `memory` `action-models` `icrl`
