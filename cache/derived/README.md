# Derived Cache Artifacts

This directory stores generated JSON helpers and intermediate outputs such as:

- reference-mining outputs
- structured note extracts
- slot extraction helpers

These files are retained for reuse, but they are not canonical workflow artifacts.
