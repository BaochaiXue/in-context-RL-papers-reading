\begin{abstract}
  Recent works have shown the remarkable superiority of transformer models in reinforcement learning (RL), where the decision-making problem is formulated as sequential generation. Transformer-based agents could emerge with self-improvement in online environments by providing task contexts, such as multiple trajectories, called in-context RL. However, due to the quadratic computation complexity of attention in transformers, current in-context RL methods suffer from huge computational costs as the task horizon increases. In contrast, the Mamba model is renowned for its efficient ability to process long-term dependencies, which provides an opportunity for in-context RL to solve tasks that require long-term memory. To this end, we first implement Decision Mamba (DM) by replacing the backbone of Decision Transformer (DT). Then, we propose a Decision Mamba-Hybrid (DM-H) with the merits of transformers and Mamba in high-quality prediction and long-term memory. Specifically, DM-H first generates high-value sub-goals from long-term memory through the Mamba model. Then, we use sub-goals to prompt the transformer, establishing high-quality predictions. Experimental results demonstrate that DM-H achieves state-of-the-art in long and short-term tasks, such as D4RL, Grid World, and Tmaze benchmarks. Regarding efficiency, the online testing of DM-H in the long-term task is 28$\times$ times faster than the transformer-based baselines.
\end{abstract}

% Recent works have shown the remarkable superiority of transformer models in reinforcement learning (RL), which formulates the decision-making problem as the sequential generation\jf{it is weird of 'which' here. suggestion: 'where the decision-making problem is formulated as the sequential generation'}. Transformer-based agents could emerge with self-improvement in online tasks by providing task contexts consisting of\jf{, such as} multiple trajectories, called in-context RL. However, due to the quadratic complexity of transformers\jf{due to the quadratic computation complexity of attention in transformers}, current in-context RL methods suffer from huge computational costs as the task horizon increases. In contrast, the Mamba model is renowned for its efficient ability to process long-term sequences\jf{whether dependencies is more suitable?}, which provides an opportunity for in-context RL to solve tasks that require long-term memory. To this end, we propose a\jf{remove 'a'} Hybrid Mamba (DM-H) that\jf{that possesses the merits of transformers and Mamba in xxx and long-term memory.} transfers the Mamba's success to RL tasks. DM-H proposes sub-goals to connect the Mamba and transformers, enabling the leverage of Mamba's computational efficiency while improving the transformer's effectiveness with long-term memory.\jf{see the suggestions of the above sentence} Specifically, DM-H first generates high-value sub-goals from long-term memory through the Mamba model. Then, \jf{we use} sub-goals \jf{to} prompt the transformer's action prediction, establishing high-quality dependencies. Experimental results show that DM-H achieves state-of-the-art \jf{in long and short-term tasks, such as D4RL, Grid World, and Tmaze benchmarks}on D4RL, Grid World, and Tmaze benchmarks. Regarding efficiency, the online testing of DM-H in the long-term task is 28$\times$ times faster than the transformer-based baselines.

% Recent works have shown the remarkable superiority of transformer models in in-context reinforcement learning (RL) by formulating the decision-making problem as the sequential generation.   
% However, due to the quadratic complexity of transformers, current in-context RL methods suffer from huge computational costs as the task horizon increases. In contrast, the Mamba model is renowned for its efficient ability to process long-term sequences, which provides an opportunity for in-context RL to solve tasks that require long-term memory. To this end, we propose a Hybrid Decision Mamba (HDM) that transfers the Mamba's success to RL tasks. HDM proposes sub-goals to connect the Mamba and transformers, enabling the leverage of Mamba's computational efficiency while improving the transformer's performance with long-term memory. Specifically, HDM first generates high-value sub-goals from long-term memory through the Mamba model. Then, sub-goals prompt the transformer's action prediction, establishing high-quality dependencies. Experimental results show that HDM achieves state-of-the-art on D4RL, Grid World, and Tmaze benchmarks over current in-context RL methods. Regarding efficiency, the online learning of HDM in the long-term task is 28$\times$ times faster than the transformer-based baselines.


% However, due to the quadratic complexity of transformers, current in-context RL methods suffer from huge computational costs as the task horizon increases. In contrast, the Mamba model is renowned for its efficient ability to process long-term sequences, which provides an opportunity for in-context RL to solve tasks that require long-term memory. To this end, we propose a Hybrid Decision Mamba (HDM) that transfers the Mamba's success to RL tasks. 
% The main challenge is leveraging Mamba's computational efficiency without sacrificing performance. Therefore, instead of simply replacing the backbone, HDM proposes sub-goals to connect the Mamba and transformers. For efficiency, sub-goals are first generated by Mamba from long-term memory. Then, sub-goals serve as prompts for the transformer's action prediction, establishing high-quality dependencies. Extensive experiments on D4RL, Grid World, and Tmaze show that HDM can outperform existing in-context RL methods and other representative baselines on both performance and efficiency.

% irl is good.   however, irl is limited to task horizon. to this end, we 
